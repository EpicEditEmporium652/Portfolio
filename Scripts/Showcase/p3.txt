local ss = game:GetService("SoundService")
local fc = ss:WaitForChild("fc")
local ffd = ss:WaitForChild("ffd")
local fcv = fc.Volume
local tws = game:GetService("TweenService")
local sg = require(game.ReplicatedStorage.Notification).sg
local i = workspace:WaitForChild("Buttonu1"):WaitForChild("Indicator"):WaitForChild("Glass")
local sc = workspace:WaitForChild("Screen")
local fr = sc:WaitForChild("SurfaceGui"):WaitForChild("TextLabel")
local fdr
for _, v in ipairs(sc.SurfaceGui:GetChildren()) do
if v:IsA("TextLabel") and v ~= fr and v:FindFirstChild("UIStroke") then
fdr = v
break
end
end
game.ReplicatedStorage.p3.OnClientEvent:Connect(function()
i.Material = Enum.Material.Neon
i.Color = Color3.fromRGB(0, 255, 0)
task.delay(1.3, function()
fr.Text = "<< Start Engaged >>"
fdr.Text = "<< Protocol T-SD-02 activated >>"
fr.TextSize = 64
coroutine.wrap(function()
task.wait(0.4)
for v = 1,4 do
fr.Text = ""
task.wait(0.4)
fr.Text = "<< Start Engaged >>"
task.wait(0.4)
end
end)()
end)
ffd:Play()
task.delay(6, function()
local txt = "Attention all facility personnel! Protocol T-SD-02 has been\nactivated. Full Facility Destruction will be ready for engaging in\nT - 5 minutes! Please evacuate, immediately!"
sg(txt, 13)
end)
task.wait(20.65)
ffd.Ended:Wait()
task.wait(0.1)
fr.Text = "<< Preparing FFD for engaging.. >>"
fr.TextSize = 44
for v = 19,0,-1 do
fc:Play()
fdr.Text = "<< FFD will be ready for engaging in T - " .. v .. "s >>"
task.wait(1)
end
fr.Text = "<< FFD is now ready for engaging >>"
fdr.Text = "<< Awaiting confirmation signal >>"
end)